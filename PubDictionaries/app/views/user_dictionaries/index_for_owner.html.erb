<div class="user_dictionaries_page">
  <div class="wice-grid">
    <%= 
      dump_filter_parameters_as_hidden_fields(@grid_user_dictionaries)

      grid(@grid_user_dictionaries, html: {class: "my-grid"}, header_tr_html: {class: "my-header"}, hide_submit_button: true, hide_reset_button: true ) do |g|
        g.blank_slate do
          "There are no records" 
        end
        g.column name: "Collaborator"  do |user_dic| 
          User.find_by_id(user_dic.user_id).email
        end
        g.column name: "Updated At", attribute: "updated_at", filter: false do |user_dic| 
          user_dic.updated_at
        end
        
        # g.column name: "Creator", attribute: "creator" do |dic| 
        #   dic.creator
        # end
        # g.column name: "Visibility", attribute: "public" do |dic| 
        #   dic.public == true ? "Public" : "Private" 
        # end
        # g.column name: "Created At", attribute: "created_at", filter: false do |dic| 
        #   dic.created_at
        # end
        # # g.column name: "Description", attribute: "description" do |dic| 
        # #   dic.description
        # # end
        # g.column name: "Functions", filter: false do |dic|
        #   link_to(image_tag('patch.jpg', size: "16x16", alt="Show related user dictionaries"), edit_dictionary_path(dic)) \
        #   + link_to(image_tag('mac-trashcan.jpg', size: "16x16", alt="Delete"), dic, method: :delete, data: { confirm: 'Are you sure to completely delete this dictionary?' })

        #   # show_link = link_to('Show', dic)
        #   # dele_link = link_to('Delete', dic, method: :delete, data: { confirm: 'Are you sure?' })
        #   # "#{show_link} | #{dele_link}".html_safe     # html_safe is necessary to ensure rendering
        #   # "#{dele_link}".html_safe     # html_safe is necessary to ensure rendering
        # end
      end
    %>
  </div>
  <br />

</div>

