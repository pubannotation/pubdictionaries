<!DOCTYPE html>
<html>

<head>
  <title>PubDictionaries</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body class="pubdictionaries">
  <div class="banner">
    <div class="logo">
  	 <%= image_tag("logo.png", :alt => "logo")%>
    </div>
    <div class="page_title">
  	 <%= @page_title || "PubDictionaries" %>
    </div>
  </div>

  <div class="authentication_information">
    <% if user_signed_in? %>
      You are logged in as <i><%= current_user.email %></i> | 
      <%= link_to "Edit profile", edit_user_registration_path %> | 
      <%= link_to "Logout", destroy_user_session_path, method: :delete %>
    <% else %>
      Please login first | 
      <%= link_to "Sign up", new_user_registration_path %>
      <%= link_to "Login", new_user_session_path %>
    <% end %>    
  </div>

  <div class="columns">
  	<div class="side">
      <div class="links">
        <%= link_to "Top", root_path %>
        <%= link_to "Manual", manual_index_path %>
        <%= link_to "Dictionaries", dictionaries_path %>
        <%= link_to "For Developers", web_services_index_path %>
        <a href="mailto:priancho@gmail.com">Contact</a>
        <br />
      </div>
  	</div>
    
    <div class="main">
      <% if notice %><p><%= flash[:notice] %></p><% end %>
      <% if alert %><p><%= alert %></p><% end %>

      <%= yield %>
    </div>
  </div>
</body>

</html>
