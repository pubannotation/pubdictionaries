<!DOCTYPE html>
<html>

<head>
  <title>PubDictionaries</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= yield :css -%>
  <%= javascript_include_tag "application" %>
  <%= yield :javascript -%>
  <%= csrf_meta_tags %>
</head>

<body>
  <div class="header">
    <div id="logo">
      <%= link_to image_tag("logo.png", { width: "100%", border: "0", alt: "Logo" }), root_path %>
    </div>
    <div id='cssmenu'>
      <ul>
        <li><%= link_to 'Dictionaries', dictionaries_path %></li>
        <li><%= link_to 'Find IDs', find_ids_path %></li>
        <li><%= link_to 'Text Annotation', text_annotation_path %></li>
        <li class='last'><%= link_to 'About', about_path %></li>
      </ul>
    </div>
    <div class="account">
      <% if user_signed_in? %>
        <%= link_to current_user.username, show_user_path(current_user.username) %> |
        <%= link_to "Logout", destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to "Sign up", new_user_registration_path %> |
        <%= link_to "Login", new_user_session_path %>
      <% end %>
    </div>
  </div>
  <% if notice %><div class='message'><span class='notice'><%= flash[:notice] %></span></div><% end %>
  <% if alert %><div class='message'><span class='alert'><%= alert %></span></div><% end %>

  <div class="main">
    <%= yield %>
  </div>

  <div class="footer">
    <div class="alert">It is now in a test service. You can find the current official service at <%= link_to 'http://pubdictionaries.org', 'http://pubdictionaries.org' %>.</div>
    <div class="credit">Presented by <%= link_to 'DBCLS', 'http://dbcls.rois.ac.jp' %></div>
    <div class="contact">We appreciate your any comment to <%= mail_to 'admin@pubdictionaries.org' %></div>
  </div>
  <%= yield :bottom_js -%>
</body>
</html>
